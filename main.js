var minimumRange = document.querySelector('leftsection__range--minimum');

minimumRange.addEventListener('click', function();



function getUserName() {
  var minRangeString = document.getElementByID('tktktk').value;
  var minRangeNumber = parseInt(minRangeString);
}

// if (nameField.lenght <3) {
//   result.textContent = 'Please select a smaller number';
// }

function clickButton() {
  var dogName = document.getElementById('dog-name-input').value;
  document.querySelector('.bold').innerText = dogName;
}